{{> agencyheader}}

<div class="container-fluid detailscon">
    <h2 class="page-header">Packages</h2>
    <div><a class="btn btn-primary" href="/agency/package/add" role="button">Add New</a></div>
</div>
<div class="topgap">
<table id="example" class="table accountstable table-striped table-bordered" cellspacing="0" width="100%">
    <t>
        <tr>
            <th>Name</th>
            <th>Tour Length</th>
            <th>Departure Location</th>
            <th>Departure Time</th>
            <th>Cost ($) - Adult</th>
            <th>Cost ($) - Child</th>
        </tr>
    </thead>
    <tfoot>
        {{#print}}
        <tr>
            <td>{{name}}</td>
            <td>{{tourlength}}</td>
            <td>{{departurelocation}}</td>
            <td>{{departuretime}}</td>
            <td>{{costadult}}</td>
            <td>{{costchild}}</td>
            <td><a class="btn btn-success" href="/agency/package/{{id}}" role="button">Edit</a></td>
        </tr>
        {{/print}}
    </tfoot>
</table>
</div>